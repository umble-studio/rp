@namespace Rp.Phone.UI.Components
@inherits Sandbox.UI.Panel

<root class="@Root">
	@if ( !string.IsNullOrEmpty( Icon ) )
	{
		<iconify Icon="@Icon" Color="@IconColor" Size="@(24)"></iconify>
	}
	@if ( !string.IsNullOrEmpty( Text ) )
	{
		<AppLabel Text="@Text"></AppLabel>
	}
</root>
