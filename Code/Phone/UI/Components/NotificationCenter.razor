@namespace Rp.Phone.UI.Components
@using Rp.Phone
@using Sandbox.UI
@using Phone = Rp.Phone.Phone
@inherits Panel

<root>
	@foreach ( var notification in Phone.Current.Notification.PendingNotifications )
	{
		<Notification @key="@notification" Pending="@notification"></Notification>
	}
</root>
